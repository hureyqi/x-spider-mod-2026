实时更新：v26.0.9，重构【账号与 Cookie 池】，新增【内置浏览器自动登录提取】与【手动编辑/粘贴 Cookie】双模式支持。

## 主要更新
- 内置浏览器自动登录：
  - 点击「添加新 Cookie / 账户 → 自动登录」将在内置窗口中打开 X 登录页。
  - 完成登录后自动抓取凭据（auth_token / ct0），拼装为标准 Cookie 并写入 Cookie 池、自动关闭窗口，零门槛使用。
  - 登录窗口使用独立 WebView2 数据目录，并跟随软件代理设置（自定义代理 / 系统代理 / 关闭代理），解决代理环境下登录页无法加载的问题。
- 手动输入与编辑：
  - 手动模式支持直接粘贴整段 Header Cookie 字符串，或单独录入 auth_token / ct0 自动拼装。
  - 保存前对格式做合规校验（缺少必需字段时给出明确提示）。
  - 已保存账户新增「✎ 编辑」按钮，可随时更新过期 Cookie 或修改备注。
- 弹窗改为双 Tab（自动登录 / 手动输入），与左侧栏账户、设置页账户池统一共享状态。

## 说明
- 便携版 \`x-spider.exe\` 已随本 Release 提供，下载后可直接运行（请确保 \`aria2c\` 就绪）。
- 安装包：\`X-Spider-二黑修改版_26.0.9_x64-setup.exe\`。