实时更新：v26.3.0，批量下载新增「失败验证」功能，可针对下载失败的博主地址进行二次验证并重新创建任务；已完成 / 失败的博主地址（@XXXX）支持点击直接打开对应主页。

## 主要更新
- 批量下载新增「失败验证」：
  - 在「重置」按钮右侧、「失败」列表上方新增「失败验证」按钮（红色主题 + 重试图标）。
  - 点击后对全部失败账户依次重新获取用户信息并创建下载任务；验证成功的账户自动从失败列表移入已完成，失败计数同步扣减，日志实时记录每一步结果。
  - 验证期间按钮呈加载态并防重复触发；批量下载进行中 / 暂停中或失败列表为空时按钮自动禁用。
  - 单个账户验证成功 / 失败均给出应用内通知与系统通知提醒。
- 批量下载博主地址可点击打开：
  - 「已完成」与「失败」列表中的 @XXXX 地址标签支持点击，调用系统默认浏览器直接打开对应的 Twitter 主页（https://twitter.com/XXXX），悬停标签可查看完整链接提示。

## 说明
- 便携版 `x-spider.exe` 已随本 Release 提供，下载后可直接运行（请确保 `aria2c` 就绪）。
- 安装包：`X-Spider-26.3.0-x64-setup.exe`。
