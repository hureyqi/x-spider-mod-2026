实时更新：v26.0.8，主要新增账号与多 Cookie 轮换机制，并完善相关管理 UI。

## 主要更新
- 多 Cookie 轮换：新增 Cookie 池管理与自动轮换，遇到 429/401 时自动切换 Cookie 并配合指数退避重试（2000ms / 4000ms / 8000ms），降低被封概率，提升抓取稳定性。
- 账号与 Cookie 池管理：
  - 设置页新增「账号与 Cookie 池」专区，支持添加、删除、校验 Cookie 有效性，及轮换开关。
  - 左侧栏顶部账户下拉菜单支持多账号切换与快捷入口，与设置页实时联动（统一全局 Store）。
- 批量下载列表增强：支持列表的导入 / 导出 / 重命名。
- 侧边栏新增「当前任务」小部件：有任务时显示迷你进度卡，点击可跳转到下载管理页，无任务时自动隐藏。
- 新增后处理编排入口（LLM 打标 / 副进程调度）。
- 移除关于页「原作者」信息。

## 说明
- 旧版单 Cookie 会自动迁移为账号 1。
- 便携版 \`x-spider.exe\` 已随本 Release 提供，下载后可直接运行（请确保 \`aria2c\` 安装或就绪）。