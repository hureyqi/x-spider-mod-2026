# v26.4.0

优化几千个批量下载任务的卡顿问题，并提升进度显示的准确性。

## 性能优化

### 数据层增量更新
- `stores/download.ts` 新增 gid → 数组下标缓存，`updateDownloadTask` 从全量 `findIndex` 线性查找降为 O(1) 定位。
- `batchUpdateDownloadTasks` 仅对「确实发生变化的 gid」构建更新映射并替换对应项；无变化时不再触发 `set`，避免每 500ms 无谓的全量重渲染。
- `scheduleAutoSyncTasks` 不再对全量任务数组做 `map` + `Promise.all`，改为只遍历屏幕上可见的 gid 精确更新。

### 批量下载界面日志/标签限量
- 日志数组限量到最近 300 条，杜绝随几千账户无限增长的数组复制。
- 已完成 / 失败 @ 标签限量渲染最近 50 个，其余以「及另外 N 个」占位，避免几千个标签一次性挂载到 DOM。

## 修复
- 提升下载详细进度与解析进度在数千任务场景下的显示准确性。